APP_PORT=8080
DATABASE_NAME=postgres
DATABASE_USERNAME=postgres
DATABASE_PASSWORD=postgres
DATABASE_PORT=5432
# Use `db` inside docker-compose and `localhost` for local non-Docker runs.
DATABASE_HOST=localhost
